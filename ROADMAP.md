# 路线图 · ROADMAP

> 记录当前进度、已知问题与计划功能。版本更新日志见 [CHANGELOG.md](CHANGELOG.md)。

## 当前状态

- 最新版本：**v1.3.1**（2026-08-09，标签体系 + 快捷键速查浮层溢出修复）
- 测试：`npm test` 51 个全部通过（含多开写锁、令牌、任务落盘、WebP 400、标签净化/批量/标签云、running 任务取消、损坏 EXIF 兜底回归）；`npx electron scripts/ui-smoke.cjs` 全绿（`CONSOLE-ERRORS []`，新增 `WEBP400`、`TAG` 用例）

## 已完成

- 相册浏览：拖拽/点击上传、WebP 缩略图、**瀑布流布局**、搜索、排序、筛选（星级/标记/格式）、灯箱、幻灯片
- 编辑器：6 种滤镜预设、亮度/对比度/饱和度/色相/锐化/模糊/黑白、撤销重做（Ctrl+Z / Ctrl+Y）、旋转/翻转/交互式裁剪、像素与百分比缩放、JPEG/PNG/WebP/AVIF 导出（保存副本 / 覆盖原图 / 下载到本地）
- EXIF：查看、编辑（JPEG / PNG / WebP，支持 UTF-8/中文，无损写入）、无损抹除
- 中文兼容：上传文件名按 UTF-8 还原（启动时自动修正历史乱码文件名）；EXIF 文本从 TIFF 字节解析，按 BOM → UTF-8 → GBK 智能解码，外部软件写的 UTF-16 / GBK 中文不再乱码
- 预览修复：网格卡片复选框与像素尺寸角标不再重叠；信息页/编辑器从侧边栏进入时自动加载当前照片（预览不再空白）；信息页预览改为棋盘格底 + 图片白边，浅色/白色图片清晰可见
- 选片：1-5 星评分、精选/排除标记、批量评分/标记/删除/ZIP 打包、收藏夹管理；**X/U 快速选片快捷键**、标记后自动跳转（可关闭）、**隐藏排除快速筛选（H）**
- **标签体系**：一图多标签（任意文本，支持中文）、信息页标签编辑（chips 增删）、工具栏标签筛选、批量添加标签、标签云计数（收藏夹之外的第二组织维度）
- 批量处理：多选统一应用预设/缩放/格式/质量，后台队列 + 进度条 + 逐张错误隔离 + 可取消（另存副本或覆盖原图）
- 图片对比：编辑器**前后对比**（可拖动分界线）+ 灯箱**并排对比选片**（C 键，Tab 切换标记目标）
- 系统：设置持久化、日志系统（实时刷新/过滤/清空）、OOBE 首次引导、GSAP 动效（支持减弱动效偏好）、Inno Setup 安装脚本、**便携版数据跟随 exe**

## 计划中

- [ ] 编辑版本链：每张照片保留原图 + 历史版本，可回退/对比（非破坏式编辑）
- [ ] 时间线 / 日历浏览：按拍摄/导入时间聚合浏览
- [x] 标签体系：一图多标签 + 标签筛选（收藏夹之外的第二维度）— v1.3 已实现
- [ ] 回收站 + 全库备份/恢复：删除可恢复，一键导出 db + 原图
- [ ] 更多 EXIF 字段编辑（GPS、拍摄参数等）
- [ ] 便携版与安装版数据目录互相迁移工具

## 已知问题

- 无公网认证：Electron 生产写请求有本地令牌校验（`X-Luma-Token`），但设计仍用于本地/个人使用，不应直接暴露公网
- 多开共享同一数据目录时，写操作靠文件级写锁兜底（冲突返回 409），但两个实例同时编辑仍应避免（建议「只看不编」）
- PNG/WebP 的 EXIF 写入依赖文件块结构，损坏或不支持的变体（如部分动画 WebP）会安全拒绝并提示（v1.2.1 起文案友好）
- 关闭动画偏好（prefers-reduced-motion）时 GSAP 动效自动降级为 CSS 过渡
