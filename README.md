# Luma Studio - Electron Releases

此分支用于存放 Electron 桌面版的构建产物（.exe 安装包 / 便携版）。

## 当前状态

Electron 桌面版尚未成功构建，详见 `main` 分支的 README.md。

## 构建方式（待修复后可用）

```bash
cd LumaStudio-electron
npm install
npm run build:win
```

构建产物将输出到 `release/` 目录。
